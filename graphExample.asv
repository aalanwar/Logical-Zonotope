clear all

close all


n_x = 5;
n_u = 4;
G = [];
for i =1:n_u
    G = [G round(rand(n_x))];
end

% number of points in x
n_x_points = 2;
pointx =[];
for i =1 :n_x_points
xx = zeros(n_x,1);
xx(randi(n_x,1)) =1;
pointx = [pointx xx];
end
logX= logicalZonotope.enclosePoints(pointx);

% number of points in u
n_u_points = 2;
for i =1 :n_u_points
xx = zeros(n_x,1);
xx(randi(n_x,1)) =1;
pointu = [pointu xx];
end
logX= logicalZonotope.enclosePoints(pointx);


